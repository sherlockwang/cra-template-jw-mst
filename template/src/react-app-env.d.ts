/// <reference types="react-scripts" />

/** 
 * Instead globals.d.ts
 * can put react app globals var definition here 
 * */
function If(condition: any): any
var Choose: any
function When(condition: any): any
var Otherwise: any
function For(each: any, of: any): any

interface Window {
  __REDUX_DEVTOOLS_EXTENSION__?: any
}
